require("csa_base")
unpack()

illegal = {
	COORDS(F,3),
	COORDS(M,3),
	COORDS(F,4),
	COORDS(L,4),
	COORDS(D,8),
	COORDS(I,8),
	COORDS(I,9),
	COORDS(H,10),
	COORDS(I,10),
	COORDS(D,13),
	COORDS(E,14),
}

init_csa()